name: Security

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  security:
    uses: fjacquet/ci/.github/workflows/go-security.yml@v1 # nosemgrep: github-actions.security.third-party-action-not-pinned-to-commit-sha
