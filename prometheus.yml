global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/license.rules.yml

scrape_configs:
  - job_name: vmware_licenses_exporter
    metrics_path: /metrics
    static_configs:
      - targets: ["vmware_licenses_exporter:9106"]
