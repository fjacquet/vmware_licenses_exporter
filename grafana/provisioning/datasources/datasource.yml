# Grafana datasource provisioning for the docker-compose test stack.
# Points Grafana at the Prometheus service that scrapes vmware_licenses_exporter.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
